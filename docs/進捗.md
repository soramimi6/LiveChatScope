# LiveChatScope — 進捗・開発ガイド

> **更新**: 2026-06-22（G-05 却下、chat-downloader 監視 v1 完了）
> **GitHub**: https://github.com/soramimi6/LiveChatScope

---

## 1. 現在地

| 項目 | 値 |
|------|-----|
| 統合ブランチ | `dev` @ `da91fac` |
| リリース | `master` — **第四弾**まで反映済 |
| 次 | Phase D（UX-20 設定永続化は D3） |

**第四弾までに入っている主な領域**: Phase A/A+ 分析一式、UX Phase 0〜5、50k+ 性能最適化、フィルター・グラフ UI、中断再開・再試行 API。

設計・仕様の詳細は [概要.md](概要.md) / [アーキテクチャ.md](アーキテクチャ.md) / [API仕様.md](API仕様.md) / [UI仕様.md](UI仕様.md) を参照。

---

## 2. 未着手 backlog

| 優先 | 内容 | メモ |
|:----:|------|------|
| — | **UX-20** 設定永続化 | Phase D — [要件.md §Phase D](要件.md) 認証仕様 **確定**（D3 で実装） |

**Phase B**: 完了（性能・中断再開・エラー強化・**chat-downloader 監視 v1**）。

**chat-downloader 監視（v1 完了）**

| 項目 | 値 |
|------|-----|
| 方式 | commit hash 固定 ＋ 手動軽量チェック（スクリプト・CI・`/health` 拡張は v1 対象外） |
| fork / pin | [Indigo128/chat-downloader](https://github.com/Indigo128/chat-downloader) @ `aed69e336fed4f587d7d0dea90a0b3c4732dd776` |
| 固定テスト動画 | https://www.youtube.com/watch?v=HFRMcvNxyH0（`HFRMcvNxyH0`） |
| 合格条件 | 1 件以上メッセージ取得 / タイムアウト 60 秒 |
| 実行タイミング | リリース前 / pin 更新後 / 取得障害の疑い |
| 失敗時 | Issue **手動**起票（`gh issue create` 例は [backend/README.md](../backend/README.md)） |
| 定数 | `backend/app/monitor_config.py` |
| 手順 | [backend/README.md §chat-downloader 監視](../backend/README.md#chat-downloader-監視v1) |

**取得の途中再開**（チャット再取得を中断地点から再開）は chat-downloader 都合で未実装。現状は全件再取得 or 分析のみ再開（`POST /api/v1/videos/{id}/retry`）。

**却下・見送り**

| ID | 内容 |
|----|------|
| G-05 | `partial` 分析状態の活用（段階的表示・再開 UX の拡張） — **却下** |

---

## 3. 開発環境

### セットアップ

```powershell
git clone https://github.com/soramimi6/LiveChatScope.git
cd LiveChatScope
git checkout dev && git pull origin dev

# Backend
cd backend
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt -r requirements-dev.txt
uvicorn app.main:app --reload --port 8000

# Frontend（別ターミナル）
cd frontend
copy .env.example .env.local
npm install
npm run dev
```

Linux/macOS: `source backend/.venv/bin/activate`

- Frontend: http://localhost:3000
- API: http://localhost:8000/docs
- DB: `backend/data/livechatscope.db`（gitignore）

### テスト

```bash
# Backend ユニット
cd backend && PYTHONPATH=. pytest tests/ -q \
  --ignore=tests/test_e2e_flow.py --ignore=tests/test_perf_api.py

# Frontend
cd frontend && npm test -- --run && npm run build

# E2E API（Backend 起動済み）
./scripts/e2e-api.sh "https://www.youtube.com/watch?v=8ZaCtuVdWYc"
```

直近: BE **90 passed**、FE **32 passed**、`npm run build` OK。

---

## 4. 作業ルール

```
master  … リリース
  ↑
  dev   … 日常統合
  ↑
  cursor/* / feat/* / fix/* … 作業ブランチ
```

- 作業ブランチは `dev` から切る
- UX 改修は **ユーザー明示指示があるまで着手しない**
- **docs に新規ファイルを増やさない**（必要ならユーザー確認）
- 取得仕様変更時は `FETCH_SPEC_VERSION` を bump（`.cursor/rules/fetch-spec-reset.mdc`）
- 話題キーワード調整は `.cursor/rules/topic-keyword-tuning.mdc` の手順に従う

---

## 5. コード早見

| 領域 | 主な場所 |
|------|----------|
| Pipeline | `backend/app/services/analysis/`（stage0–8, stage4b, refilter） |
| 取得・ジョブ | `fetch_worker.py`, `job_recovery.py`, `api/videos.py` |
| API | `backend/app/api/analysis.py`, `export.py`, `videos.py` |
| フィルター | `message_filter.py`, `global-filter-bar.tsx`, `session-filter.ts` |
| Frontend タブ | `frontend/components/tabs/` |

---

## 6. 確定方針（実装時）

| # | 内容 |
|---|------|
| Q1 | フィルターはハイブリッド — 密度・盛り上がり等は全 msg、話題・キーワード・export messages のみ除外反映 |
| Q2 | JSON = 分析一式、CSV = messages ログ |
| Q7/Q8 | フィルター変更時は Stage 4–8 を再実行（正確性最優先） |

---

## 7. 既知の制限

- chat-downloader（[Indigo128 fork](https://github.com/Indigo128/chat-downloader) @ `aed69e33…` pin 済）— YouTube 仕様変更リスク。手動チェック手順は [backend/README.md](../backend/README.md)
- 推定話題は配信内容と一致しない場合あり
- 50k+ 性能はベンチ合格済。極端な超大型配信（431k msg 級）は P-01 数値上は未達だが、ターゲット外として許容
- 認証なし POC / 日本語 UI のみ
